# 零钱簿 · 阿砚事实库

> 本文件只记录可从当前仓库或当次命令输出核验的事实。历史信息会明确标记，不把旧验证写成当前结论。

## 仓库与版本

| 项目 | 当前事实 | 状态 | 证据 |
| :--- | :--- | :--- | :--- |
| GitHub 仓库 | `https://github.com/GinFJ/lingqianbu` | 已验证 | `git remote get-url origin` |
| 分支 | `main` | 已验证 | `git branch --show-current` |
| Git 提交 | 已建立首次公开提交 | 已验证 | `git log -1 --oneline` |
| 应用版本 | `1.0.2` | 已验证 | `package.json`、`package-lock.json` |
| 技术栈 | React 19、TypeScript、Vite、Dexie、PapaParse、Recharts、Lucide React | 已验证 | `package.json` 与源码 |
| 默认存储 | 浏览器 IndexedDB | 已验证 | `src/data/db.ts` |
| 可选同步 | Supabase + 浏览器端 AES-GCM | 已验证实现 | `src/sync/`、`supabase/schema.sql` |

## 2026-08-10 · 1.0.2 P1 质量完善验证

| 检查 | 结果 | 状态 | 命令或说明 |
| :--- | :--- | :--- | :--- |
| 自动化测试 | 8 个测试文件、41 项测试通过 | 已验证 | `npm.cmd test` |
| TypeScript + 生产构建 | 成功 | 已验证 | `npm.cmd run build` |
| 数据与同步一致性 | 流水编辑删除、预算新增编辑删除、自定义分类依赖、清空数据和 CSV 导入均覆盖对应同步事件 | 已验证 | `src/App.test.tsx` |
| 键盘与表单 | 弹窗自动聚焦、Tab 圈定、Escape 关闭、焦点归还及关键输入标签通过 | 已验证 | 组件测试与本地浏览器 |
| 图表替代信息 | 六个月趋势、十二个月收支和分类占比均提供可读数值表 | 已验证 | 组件测试与浏览器 DOM |
| 高对比状态 | 当前页、选中分类、超支和进度均有颜色以外的文字、形状或语义状态 | 已验证 | 源码检查；含 `prefers-contrast` 与 forced colors 样式 |
| 图片体积 | 实际加载 WebP 为 156.09 kB + 66.60 kB，较原 PNG 约 3.74 MB 减少约 94% | 已验证 | Vite 构建输出 |
| 图表加载 | `charts` 421.65 kB（gzip 114.85 kB），首页不预载，进入报表后加载 | 已验证 | 构建 HTML与本地浏览器资源检查 |
| 桌面布局 | 1440×900，无横向溢出；总览和报表可用 | 已验证 | 隔离本地预览；0 warning、0 error |
| 移动布局 | 375×812，无横向溢出；流水、编辑弹窗和焦点可用 | 已验证 | 隔离本地预览；0 warning、0 error |
| 隔离测试数据 | `0.01` 元测试流水已删除 | 已验证 | `127.0.0.1:4174` 隔离来源 |
| 历史本地数据 | `127.0.0.1:4173` 发现旧账目后立即停止操作，未修改或清空 | 已验证边界 | 浏览器验收记录 |
| 1.0.2 发布包 | 未生成 | 未验证 | 本次未获发布授权；现有候选包仍为 1.0.1 |

## 2026-08-10 · 1.0.1 发布候选验证

| 检查 | 结果 | 状态 | 命令或说明 |
| :--- | :--- | :--- | :--- |
| 依赖恢复 | 成功，新增 256 个包 | 已验证 | `npm.cmd ci --offline` |
| npm 审计 | 0 个已知漏洞 | 已验证 | 离线安装输出 |
| 自动化测试 | 8 个测试文件、35 项测试通过 | 已验证 | `npm.cmd test` |
| TypeScript + 生产构建 | 成功 | 已验证 | `npm.cmd run build` |
| 构建产物 | `dist/index.html` 与 8 个分包/素材文件生成 | 已验证 | Vite 构建输出 |
| 发布候选 ZIP | 4,043,141 字节，SHA-256 `7a1c94ea6606c9a558769130a5f381429645a6bb6c5d8594711e139627df0484` | 已验证 | `docs/发布候选记录-1.0.1.md` |
| 桌面关键路径 | 1440×900；新增、编辑、删除、预算、报表、CSV 和同步入口可用 | 已验证 | 全新浏览器会话与最终 `dist` |
| 移动布局 | 375×812，无横向溢出；底部导航、快速记账和弹窗边界可用 | 已验证 | 全新浏览器会话与最终 `dist` |
| 浏览器控制台 | 0 warning、0 error | 已验证 | 最终 `dist` 本地预览 |
| IndexedDB 与 CSV | 首次建库、v1→v2 升级、CSV 写入和事务失败回滚通过 | 已验证 | `src/data/db.test.ts` |
| 同步失败与冲突 | 错误密钥、损坏密文、唯一 IV、重复事件、离线队列、流水冲突收敛和本机写入不受同步失败影响通过 | 已验证 | `src/sync/*.test.ts`、`src/App.test.tsx` |
| Supabase RLS 本机隔离测试 | anon、非成员、成员、跨房间、追加、篡改和删除边界通过 | 已验证 | `supabase/schema.test.ts`；PGlite PostgreSQL 18.3 |
| Supabase 云端隔离项目 RLS | 同一边界复验通过；多余默认授权已修复；测试数据清零 | 已验证 | 每月 0 美元项目 `lingqianbu-p0-rls-test`，验收后已暂停 |
| 当前线上部署 | 未执行 | 未验证 | 本次未获部署授权 |
| 当前双设备同步 | 未执行 | 未验证 | 本次未使用生产凭证或真实设备 |

## 迁入边界

2026-08-09 从旧工作目录迁入以下内容：

- React/TypeScript 源码与测试；
- 水墨界面素材；
- Supabase schema；
- package 与 TypeScript/Vite/Vitest 配置；
- 公开无效占位值 `.env.example`。

明确未迁入：

- `.env.local`；
- 生产配对链接和二维码；
- `node_modules`、`dist`、TypeScript 缓存；
- 旧部署 ZIP、源代码 ZIP、临时工作目录；
- 任何浏览器 IndexedDB 财务数据。

## 历史证据，不代表当前状态

旧工作目录在 2026-08-01 至 2026-08-02 曾完成 Cloudflare Pages 部署和双浏览器同步验收。该结果说明方案曾经跑通，但当前仓库没有重新部署，也没有复用旧生产凭证，因此不能据此宣称当前版本已完成线上验收。

最后核验：2026-08-10
