# 零钱簿项目协作规范

本文件适用于整个仓库，包括后续迁入或新增的所有源码、测试、文档、数据库脚本与交付文件。若子目录将来存在更具体的 `AGENTS.md`，则子目录规则只在其范围内补充或覆盖本文件。

## 1. 项目定位与优先级

“零钱簿”是面向中文用户的人民币个人财务网站，核心目标是让日常记账、预算和复盘简单、可信、可长期使用。

实施或评审改动时，按以下优先级做决定：

1. 保护用户隐私和真实财务数据。
2. 保证金额、日期、分类和统计结果正确。
3. 保持未启用云同步时完全本机可用。
4. 保持中文、人民币和财务手账式体验一致。
5. 同时兼顾桌面端与移动端，最低检查 375px 宽视口。
6. 在不破坏以上原则的前提下改善性能、可维护性和视觉细节。

不要为了演示效果、技术新颖性或减少少量代码而牺牲数据安全、离线能力或可理解性。

## 2. 技术与架构基线

除非用户明确要求调整架构，否则延续以下技术方向：

- React + TypeScript + Vite 构建前端应用。
- 使用 Dexie 管理浏览器 IndexedDB，将本机数据作为默认数据源。
- 使用 PapaParse 处理 CSV 导入与导出。
- 使用 Recharts 展示财务报表，使用 Lucide React 提供界面图标。
- Supabase 仅用于用户主动启用的可选跨设备同步；未配置或不可用时，本机记账功能必须继续工作。
- 同步内容在浏览器端完成端到端加密；服务端只保存无法直接读取的密文和必要元数据。

保持职责边界清晰：

- `src/lib/` 放置可独立测试的金额、统计、CSV 和阿砚逻辑。
- `src/data/` 负责默认数据与本机数据库访问。
- `src/sync/` 负责加密、配对、同步协议和远端传输。
- React Context 或等价状态层协调数据，不在展示组件中复制持久化或同步规则。
- 页面组件负责交互与呈现，不应成为财务计算或加密实现的唯一位置。
- `supabase/` 保存可审计的数据库 schema、RLS 与函数定义。

架构可以随需求演进，但重大调整必须说明动机、兼容影响、数据迁移方式和验证结果。

## 3. 数据与业务约束

- 金额在程序和持久化层一律使用整数“分”，不得用浮点数直接存储人民币金额。
- 只在输入、展示和 CSV 边界进行“元”与“分”的转换；转换必须处理两位小数、非法输入和边界值。
- 日期采用本地日历语义；CSV 日期固定为 `YYYY-MM-DD`，避免无意引入 UTC 跨日问题。
- 收支类型保持明确，CSV 中只使用 `收入` 或 `支出`。
- CSV 固定表头为 `日期,类型,分类,金额,备注`，导出使用 UTF-8 BOM，确保 Excel 可直接打开中文内容。
- CSV 导入必须先预检，再由用户确认；报告非法行和重复项，不得静默丢弃、覆盖或重复写入。
- 编辑或删除流水、预算、分类时，要同步维护依赖统计和同步事件的一致性。
- 对现有 IndexedDB schema 的变更必须提供版本升级路径；不得通过清库来规避迁移。

不得擅自迁移、合并、修改、删除或上传任何真实财务数据。真实数据操作只有在用户当次明确指定数据来源、目标和动作后才可执行，并应先提供可恢复方案和验证办法。

## 4. 隐私与安全边界

- 不读取、复制、展示或提交与当前任务无关的真实账目。
- 不提交 `.env.local`、访问令牌、Supabase 密钥、配对凭证、解密密钥、生产二维码或完整配对链接。
- `.env.example` 只能包含无效占位值；浏览器端只能使用适合公开客户端的 Supabase Publishable Key，绝不能嵌入服务端密钥。
- 配对二维码和链接等同于账簿钥匙。测试产物若包含真实凭证，必须留在忽略目录中，并在任务结束前说明如何安全处理。
- 加密必须使用经平台支持和审查的 Web Crypto 原语；不得自行设计加密算法、弱化随机数、复用 IV 或将解密密钥上传到后端。
- 更改 `src/sync/`、加密载荷、配对流程、Supabase RLS、SQL 函数或权限时，必须补充针对性测试并说明威胁边界。
- 数据清空、数据库迁移、生产部署和线上数据操作都属于高风险动作；未获用户当次明确许可不得执行。
- 日志、错误信息和测试快照不得泄露账目、密钥、配对凭证或可复用的用户标识。

发现安全问题时，先停止可能扩大影响的操作，明确影响范围和恢复方法，再处理功能性工作。

## 5. 界面与阿砚设定

- 保持米白、墨绿、朱红、水墨质感和中文财务手账的整体视觉语言。
- “阿砚”是精打细算但不评判消费的水墨砚貅账灵；文案应温和、具体、可信，不制造财务焦虑，也不冒充专业理财顾问。
- 任何重要操作都要有清晰的中文标签、状态反馈和错误提示，不依赖图标或颜色单独传达含义。
- 桌面端维持清晰的侧栏或等价导航；移动端确保底部导航、快速记账、表单和图表可用。
- 至少在 375px 宽视口检查无横向滚动、文字截断、按钮遮挡和弹窗越界。
- 遵守基本可访问性要求：语义化控件、键盘可操作、可见焦点、合理对比度和表单标签。
- 不套用与现有风格冲突的通用后台模板，不因视觉改版破坏熟悉的记账流程或信息层级。

## 6. 实施流程

开始工作前：

1. 阅读本文件、任务相关文档和当前实现。
2. 检查 Git 状态，识别并保留用户已有改动。
3. 找到真实数据流、测试和配置来源，不根据旧输出猜测当前状态。
4. 只在会实质改变结果且无法从仓库确认时向用户提问。

实施时：

- 优先修复源逻辑并补回归测试，不手工修改构建产物来掩盖问题。
- 变更保持聚焦；不要顺手重写无关代码或覆盖用户改动。
- 新增依赖前说明必要性，优先使用现有依赖和浏览器标准能力；依赖变化须同步更新锁文件。
- TypeScript 不使用无理由的 `any`、非空断言或静默异常处理。
- 用户可恢复的错误要显示明确中文提示；开发诊断信息不得直接暴露敏感数据。
- 不手工编辑 `dist/`、覆盖率结果或 TypeScript 构建缓存。
- 未经明确要求，不创建提交、不推送、不开 PR、不发布、不部署，也不操作线上环境。

Windows 环境使用与项目脚本一致的命令：

```powershell
npm.cmd install
npm.cmd run dev
npm.cmd test
npm.cmd run build
npm.cmd run preview
```

`package.json` 是脚本名称的最终事实来源。若脚本发生变化，应同步更新本文件和 README。

## 7. 按改动类型验证

只运行与风险相称的验证，但不得跳过受影响的关键路径。

### 业务逻辑

- 为金额换算、月度汇总、预算、分类统计和边界情况增加或更新单元测试。
- 覆盖零值、极大值、小数精度、跨月日期、空数据和无效输入。
- 核对界面汇总与底层计算使用同一套规则。

### IndexedDB 与 CSV

- 使用隔离的测试数据库或 `fake-indexeddb`，测试之间不得共享真实浏览器数据。
- 验证首次建库、升级、增删改查和失败恢复。
- 验证 CSV 表头、UTF-8 BOM、Excel 中文兼容、预检、非法行提示和重复识别。
- 任何迁移都要覆盖旧版本数据升级，不以删除数据库作为测试结论。

### 同步与安全

- 覆盖加密往返、错误密钥、损坏密文、唯一 IV、离线队列、重复事件和多设备冲突。
- 同步失败不得阻塞本机记账，也不得造成已确认数据消失。
- SQL 或 RLS 变化要验证未授权用户无法读取房间、成员或同步事件，授权成员只能执行设计允许的操作。
- 线上双设备验收只能在用户明确授权后进行，并使用可识别、可删除的测试流水；结束时核对两端均已清理。

### 界面与响应式

- 测试主要导航、快速记账、编辑删除、预算、报表、CSV 和同步入口。
- 手动检查桌面与 375px 移动视口，确认无控制台错误和横向溢出。
- 视觉改动要检查阿砚、水墨资产、文字层级、键盘焦点和空白/加载/错误状态。

### 文档与配置

- 文档命令、环境变量名称、CSV 示例和部署说明必须与当前实现一致。
- 文档或规范单独变更时无需运行应用测试，但要检查 Markdown、链接、命令和安全表述。
- 不把本地绝对路径写成项目运行依赖或面向用户的固定配置。

## 8. 构建、打包与发布

涉及源码的改动完成后，默认执行：

```powershell
npm.cmd test
npm.cmd run build
```

- 只有命令实际成功后才能称为“测试通过”或“构建成功”；若未运行、被阻塞或只运行了部分测试，要准确说明。
- 构建应继续使用独立的 Vite 与 Vitest 配置，避免生成的配置 `.js`、`.d.ts` 或 `*.tsbuildinfo` 污染解析。
- Cloudflare Pages 直传 ZIP 在 Windows 下使用 `tar.exe` 等能生成正斜杠条目的工具。
- 上传前检查压缩包根目录包含 `index.html`，静态资源位于 `assets/...`，且条目名不含反斜杠。
- 部署后应检查页面 DOM、脚本加载、控制台、375px 布局和受影响功能；部署与线上验收仍须用户明确授权。

## 9. 沟通与交付

- 默认使用简体中文，先给结论，再说明关键依据、风险和验证结果。
- 直接、自然地说明已完成内容；不把计划、推测或历史结果写成当前已验证事实。
- 若遇到权限、网络、人工登录或文件选择阻塞，明确告诉用户需要做什么，并保留当前可恢复状态。
- 最终交付列出实际变更文件、运行过的验证及其结果、未验证项和仍需用户决定的事项。
- 不夸大完成度，不用无证据的“全部正常”“完全安全”或“测试全绿”。

## 10. 完成标准

任务只有在以下条件满足时才算完成：

- 改动范围符合用户请求，没有夹带无关重构或外部操作。
- 数据、隐私、安全和本机优先原则未被破坏。
- 受影响逻辑有与风险相称的自动化测试或明确的人工验证。
- 涉及源码时，测试与生产构建已运行，或已如实记录无法运行的原因。
- 涉及界面时，桌面端和 375px 移动端均已检查。
- 涉及同步或数据库安全时，失败模式、权限边界和回归测试已覆盖。
- 文档、示例、环境变量和脚本与当前实现一致。
- 没有泄露密钥、配对凭证、真实账目或其他敏感数据。
- 最终说明可由用户复核，且不把未获授权的提交、推送、部署或真实数据操作视为默认步骤。

## 11. 项目治理与版本同步

本仓库复用空谷项目的长期治理分层，但所有事实、决策和质量结论必须以零钱簿当前仓库为准，不直接照搬空谷的技术栈、版本号或验证数字。

- `README.md` 是对外入口，只写当前实现、当前验证和明确限制。
- `MEMORY/事实.md` 记录当次可复核的仓库、版本、测试、构建和发布状态。
- `MEMORY/决策点.md` 记录长期有效的产品、数据、安全与架构决策。
- `MEMORY/待办.md` 记录分级未完成事项，不把计划写成已交付功能。
- `docs/变更记录.md` 记录每次正式交付；`docs/PROJECT_CHRONICLE.md` 记录有证据的里程碑和路线变化。
- 任何进入正式交付的变更完成后，同步检查 README、MEMORY、变更记录、质量门禁和项目志；只更新实际受影响的文件。
- 当前迁入基线版本为 `1.0.0`。后续修复和文档治理默认升 patch，兼容性功能新增升 minor，破坏性变更升 major。
- 版本变化至少同步 `package.json`、`package-lock.json`、README 版本徽章、`MEMORY/事实.md` 和 `docs/变更记录.md`；形成里程碑时同步项目志。
- 版本升级不等于建立标签、发布或部署；未经用户当次明确授权，仍不得提交、推送、建立标签、发布或操作线上环境。
